name: mgs_app2
description: "A new Flutter project."
publish_to: 'none' # Remove this line if you wish to publish to pub.dev
version: 1.0.0+1

environment:
  sdk: '>=3.3.3 <4.0.0'

dependencies:
  flutter_localizations:
    sdk: flutter

  dotted_border: ^2.1.0
  flutter:
    sdk: flutter
  shared_preferences: ^2.5.3
  intl: ^0.20.2
  provider: ^6.1.2
  firebase_core: ^3.5.0
  firebase_storage: ^12.3.1
  firebase_auth: ^5.3.0
  cloud_firestore: ^5.4.2
  cloud_functions: ^5.1.2
  line_icons: ^2.0.3
  image_picker: ^1.0.4
  image_cropper: ^8.0.2
  mime: ^1.0.5
  url_launcher: ^6.3.1
  flutter_stripe: ^9.5.0
  cached_network_image: ^3.3.1
  shimmer: ^3.0.0
  package_info_plus: ^8.3.0

dev_dependencies:
  flutter_test:
    sdk: flutter
  mockito: ^5.4.4
  build_runner: ^2.4.10
  test: any
  mocktail: ^1.0.3
  fake_cloud_firestore: ^3.0.3
  firebase_auth_mocks: ^0.14.2
  firebase_storage_mocks: ^0.7.0




  flutter_lints: ^3.0.0

flutter:

  uses-material-design: true

  assets:
    - assets/images/
    - resources/languages/it.json
    - resources/languages/errors/it.json
    - resources/languages/en.json
    - resources/languages/errors/en.json
    - resources/profile_pic.jpeg
  
  #fonts:
   #- family: Montserrat
   #  fonts:
   #    - asset: assets/fonts/Montserrat-Light.ttf
